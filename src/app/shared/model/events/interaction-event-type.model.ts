/** A type of user interaction as possible within Vitrivr NG. */
export enum InteractionEventType {
    QUERY_IMAGE,
    QUERY_AUDIO,
    QUERY_MOTION,
    QUERY_MODEL3D,
    QUERY_FULLTEXT,
    QUERY_TAG,
    MLT,
    CLEAR,
    REFINE,
    FILTER,
    HIGHLIGHT,
    BROWSE,
    EXPAND,
    EXAMINE,
    PLAY
}